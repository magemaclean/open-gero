{
  "name": "OpenGero",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "stack-logs",
      "command": "bash .cursor/logs.sh",
      "description": "Streams `docker compose logs -f` for the OpenGero stack (postgres, redis, api :8000, worker, web :8080)."
    }
  ],
  "ports": [
    { "name": "web", "port": 8080 },
    { "name": "api", "port": 8000 }
  ]
}
